/*
 * ErrorBound Messages
 *
 * This contains all the text for the ErrorBound component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ErrorBound';

export default defineMessages({
  header: {
    id: `${scope}.content`,
    defaultMessage: 'Có lỗi xảy ra.',
  },
});
