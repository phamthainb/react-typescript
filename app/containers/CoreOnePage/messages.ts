/*
 * CoreOnePage Messages
 *
 * This contains all the text for the CoreOnePage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.CoreOnePage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CoreOnePage container!',
  },
});
