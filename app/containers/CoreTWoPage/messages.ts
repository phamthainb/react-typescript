/*
 * CoreTWoPage Messages
 *
 * This contains all the text for the CoreTWoPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.CoreTWoPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CoreTWoPage container!',
  },
});
