/**
 * Test sagas
 */

// import { take, call, put, select } from 'redux-saga/effects';
// import layOutSaga from '../saga';

// const generator = layOutSaga();

describe('layOutSaga Saga', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
