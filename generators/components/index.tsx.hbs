/**
*
* {{ properCase name }}
*
*/
import React, { memo } from 'react';
import styled from 'styled-components';

interface Props {}

function {{ properCase name }}({}: Props) {
return (
<Styles{{ properCase name }}>
  {{ properCase name }}
</Styles{{ properCase name }}>
);
};
const Styles{{ properCase name }} = styled.div``;

export default memo({{ properCase name }});