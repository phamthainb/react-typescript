/*
*
* {{properCase name }} reducers
* make by phamthainb
*/
import * as types from './constants';
import { Actions{{properCase name }}, {{properCase name }}Store } from "./types";

const initState : {{properCase name }}Store = { };

const reducers{{properCase name }} = (state = initState, actions: Actions{{properCase name }}) => {
switch (actions.type) {
default:
return { ...state };
}
};

export default reducers{{properCase name }};