/*
 *
 * About
 *
 */

import React, { memo } from "react";
import ErrorBound from "@components/ErrorBound";

interface Props {}

function About({}: Props) {
  return <ErrorBound>About</ErrorBound>;
}
export default memo(About);
