/*
 *
 * Home
 *
 */

import React, { memo } from "react";
import ErrorBound from "@components/ErrorBound";

interface Props {}

function Home(props: Props) {
  return <ErrorBound>Home</ErrorBound>;
}
export default memo(Home);
