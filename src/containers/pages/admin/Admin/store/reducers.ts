/*
*
* Admin reducers
*
*/
import * as types from './constants';


const initState = { };

const reducersAdmin = (state = initState, actions: any) => {
switch (actions.type) {
default:
return { ...state };
}
};

export default reducersAdmin;