/*
*
* ManagerEvents
*
*/

import React, { memo } from 'react';
import ErrorBound from '@components/ErrorBound';


interface Props {}

function ManagerEvents({}: Props) {

return (
<ErrorBound>
  ManagerEvents
</ErrorBound>
);
}
export default memo(ManagerEvents);