/*
*
* ManagerEvents reducers
*
*/
import * as types from './constants';


const initState = { };

const reducersManagerEvents = (state = initState, actions: any) => {
switch (actions.type) {
default:
return { ...state };
}
};

export default reducersManagerEvents;