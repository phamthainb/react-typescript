/*
 *
 * Informations
 *
 */

import React, { memo } from "react";
import ErrorBound from "@components/ErrorBound";

interface Props {}

function Informations({}: Props) {
  return <ErrorBound>Informations</ErrorBound>;
}
export default memo(Informations);
