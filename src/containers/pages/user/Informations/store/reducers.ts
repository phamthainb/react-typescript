/*
*
* Informations reducers
*
*/
import * as types from './constants';


const initState = { };

const reducersInformations = (state = initState, actions: any) => {
switch (actions.type) {
default:
return { ...state };
}
};

export default reducersInformations;